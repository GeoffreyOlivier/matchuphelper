# Rename this file to `.env` and fill in your secrets.
# Make sure `.env` is NOT committed (already ignored by .gitignore).

# OpenAI
OPENAI_API_KEY=sk-your-key-here

# Optional Firebase emulators or other envs can go here.
