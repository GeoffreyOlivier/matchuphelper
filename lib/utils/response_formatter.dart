String formatMatchupResponse(String rawResponseOrJson) {
  // For now, return raw content; UI already expects formatted text.
  // You can enhance this to pretty-print from parsed JSON.
  return rawResponseOrJson;
}
